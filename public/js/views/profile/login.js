/* global ko */

var wallpaper = wallpaper || { };
wallpaper.profile = wallpaper.profile || { };
wallpaper.profile.login = wallpaper.profile.login || { };

wallpaper.profile.login.Model = (function (ko) {
  "use strict";

  var Model = function(data) {

  };

  return Model;
}(ko));
